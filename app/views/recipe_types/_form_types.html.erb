<% @recipe_type.errors.full_messages.each do |msg| %>
  <%= msg %>
<% end %>

<%= form_for @recipe_type do |f| %>
  <%= f.label :name, 'Novo Tipo de Receita' %>
  <%= f.text_field :name %>
  <%= f.submit 'Enviar' %>
<% end %>