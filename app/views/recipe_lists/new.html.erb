<% @recipe_list.errors.full_messages.each do |msg| %>
  <%= msg %>
<% end %>

<%= form_for @recipe_list do |f| %>
  <%= f.label :name, 'Nome da lista' %>
  <%= f.text_field :name %>
  <%= f.submit 'Enviar' %>
<% end %>

<%= link_to 'Voltar', root_path, class: 'botao_voltar' %>