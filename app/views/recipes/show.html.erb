
  <h1><%= @recipes.tittle %></h1>
  <ul>
    <li>Tipo de Receita: <%= @recipes.recipe_type.name %></li>
    <li>Tipo de Cozinha: <%= @recipes.cuisine %></li>
    <li>Dificuldade: <%= @recipes.difficulty %></li>
    <li>Tempo de Preparo: <%= @recipes.cook_time_min %></li>
  </ul>

  <h3>Ingredientes</h3>
  <p><%= @recipes.ingredients %></p>
  <h3>Modo de Preparo</h3>
  <p><%= @recipes.cook_method %></p>
  <br>
  <p>Receita enviada por <%= @recipes.user.email %>.</p>
  <br>

  <% if user_signed_in? && current_user.recipe_owner?(@recipes) %>
    <%= link_to 'Editar Receita', edit_recipe_path %>
  <% end %>

  <%= link_to 'Voltar', root_path, class: 'botao_voltar' %>